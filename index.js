/**
 * Service entry point
 * use node index.js -p <port> -m <service name(s)> -e <environment> to start
 */
'use strict';

// include header
require('./header');

// config
console.log(_CONF, _LIB);
